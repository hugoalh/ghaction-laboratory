on:
  workflow_dispatch:
jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "hugoalh/send-discord-webhook-ghaction@bundler/ncc-minify"
        with:
          key: "test/test"
          payload: |
            content: "Hello, world!"
            wait: true
