on:
  workflow_dispatch:
jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "hugoalh/send-discord-webhook-ghaction@bundler/ncc-minify"
        with:
          key: "12345678/test"
          payload: |
            content: "Hello, world!"
            wait: true
